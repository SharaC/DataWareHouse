const config = {
    appPort: 3030,
    HOST_DATABASE: 'localhost',
    NAME_DATABASE: 'datawarehouse',
    USER_DATABASE: 'usuariodb',
    PASSWORD_DATABASE: 'sucontraseniadeusuariodb',
    apiVersion: "/v1",
    SECRET: 'DATAWAREHOUSE-SHARAC'
}

module.exports = config;
